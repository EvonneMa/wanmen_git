第五次课：numpy 
	1.numpy的array是默认按行优先的，所以选取行的操作会很简单。
	2.np.bincount(array) #返回一个数组arr[array.max+1],其中arr[i] = 元素i在array中出现的次数
	3.arr.transpose([axis0,axis1,...]) #正常顺序为[0,1,2...],若为[0,2,1]则表示交换轴1和轴2，以此类推
	4.x,y = np.meshgrid(arr1,arr2) #画网格，先画垂直于x轴的网格线，再画垂直于y轴的网格线，gg
	5.np.where(cond,arr1,arr2)
	6.np.in1d(tar_arr,arr) #tar_arr是否是arr中的元素
	7.np.unique(arr) #返回剔除了重复元素后的arr
	8.np.save('文件名.npy',arr) #单数组
	  np.savez('文件名.npy',name1 = arr1,name2 = arr2)
	  arr = np.load('文件名.npy') #多数组
	  arr1 = np.load('文件名.npy')['name1']
	  