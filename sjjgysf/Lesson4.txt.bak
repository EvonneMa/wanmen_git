第四次课	查找与排序
	1.顺序查找O(n)、二分查找O(lgn)
	2.冒泡排序：每次都是相邻两元素比较,稳定,O(n^2)——如果待搜索数组是有序的,就变成了插入排序
	3.选择排序：每次搜索当前元素之后的所有元素中的最小元素且要比当前元素小,不稳定,O(n^2)
	4.插入排序：从第二个元素开始,依次和前面的元素比较,满足条件即交换位置,稳定,O(n^2)
	*多说一句：算法的思想有时候不适合直接搬到程序里。
			   比如插入排序,如果非要先找到位置再insert,效率反而会低
			   再比如回溯算法,有时候也不能真的严格按照回溯的定义来
			   算法和真正的实现还是有区别的
			   *希尔排序
	5.计数排序：将数字分类放在抽屉里,再按顺序依次拿出来,不稳定,空间复杂度较大,O(n)
	6.归并排序：采用分治策略：
					分——不断二分直到分为1或2个元素
					治——对每个小元组内部排序,合并成大元组后继续排序,直到对原数组排序完成
				稳定,O(nlgn),空间复杂度O(n)
				*在治的部分可以采用插入排序以提高速度
				 在治的部分可以提前查看一下前半部分[-1]是否小于后半部分的[0],如果是就可以直接合并了
	7.快速排序：选择一个pivot,将数组分成左(大数)、右(小数)两部分,递归地进行下去,直到不能再分
				O(nlgn)~O(n^2),不稳定
	8其他排序算法：二叉树搜索、堆排序等等