第八次课	堆栈和队列
	1.堆栈：当使用链表来实现的时候,可以考虑push()的元素放在head之后.也就是说,把链表的首指针而不是尾指针当做堆栈的top
	2.队列：循环数组或链表;队列需要有固定的长度,不能无限放;取余操作放置元素
	3.队列实现堆栈——只需要一个队列即可;堆栈实现队列——需要两个堆栈
	4.实时显示堆栈最小值：push()和pop()的时候对最小值数组进行维护
	5.一个数组实现两个堆栈：一头一尾
	  一个数组实现三个堆栈：开放式问题,可以分为三份,也可以用指针来解决(因为数组本身就是个指针)
	6.堆栈排序：建立一个新堆栈a、原堆栈b.
							temp = b.pop()
							if temp >= a.pop():
									a.push(temp)
							else:
									while(a.top() > temp):
											b.push(a.pop())
							a.push(temp)
							相当于递归的解决问题
	7.判断括号配对问题(stack)
	8.类似计算器的问题都可以考虑堆栈或队列这类数据结构