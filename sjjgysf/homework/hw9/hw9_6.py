# 最长单词
# 该单词可由更短的单词通过添加一个字母得到,如 a->ap->app->appl->apple
def findLongestWord(s):
	res = set()
	s.sort() #很有意义的排序
	for ele in s:
		if len(ele) = =1 or ele[:-1] in dic:
			res.add()
			resword = word if resword == '' else word if len(word) > len(resword) else resword
	return resword
	